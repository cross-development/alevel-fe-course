// Packages
import { FC, memo } from 'react';

const UserDetailsView: FC = memo(() => {
  return <div>UserDetailsView</div>;
});

UserDetailsView.displayName = 'UserDetailsView';

export default UserDetailsView;
