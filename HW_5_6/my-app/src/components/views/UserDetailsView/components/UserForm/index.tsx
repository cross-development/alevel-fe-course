// Packages
import { FC, memo } from 'react';

interface Props {}

const UserForm: FC<Props> = memo(() => {
  return <div>UserForm</div>;
});

UserForm.displayName = 'UserForm';

export default UserForm;
