// Packages
import { FC, memo } from 'react';

const ResourceDetailsView: FC = memo(() => {
  return <div>ResourceDetailsView</div>;
});

ResourceDetailsView.displayName = 'ResourceDetailsView';

export default ResourceDetailsView;
