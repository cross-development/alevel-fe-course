// Packages
import { FC, memo } from 'react';

interface Props {}

const SignInForm: FC<Props> = memo(props => {
  return <div>SignInForm</div>;
});

SignInForm.displayName = 'SignInForm';

export default SignInForm;
